{%- comment -%}

++++++++++++++++++++++++++++++++++
+ Site settings override
+
+ Override gcweb-jekyll theme settings to use site.baseurl for proper
+ PR preview support. When baseurl is set (e.g., /pr-preview/pr-123),
+ breadcrumb links will correctly include the preview path.
++++++++++++++++++++++++++++++++++

{%- endcomment -%}
{%- assign setting-packageName = "GCWeb" -%}
{%- capture setting-resourcesBasePathTheme -%}
	https://wet-boew.github.io/themes-dist/GCWeb/{{ setting-packageName }}
{%- endcapture -%}
{%- assign setting-resourcesBasePathWetboew = "https://wet-boew.github.io/themes-dist/GCWeb/wet-boew" -%}
{%- comment -%}
Override siteBasePath to use Jekyll's baseurl for proper PR preview support.
When site.baseurl is empty (production), this resolves to just the site.url.
When site.baseurl is set (PR preview), links include the preview path.
{%- endcomment -%}
{%- capture setting-siteBasePath -%}{{ site.baseurl }}{%- endcapture -%}
{%- assign setting-demosBasePath = "https://wet-boew.github.io/gcweb-compiled-demos/" -%}
